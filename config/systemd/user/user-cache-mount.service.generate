[Unit]
Description=Create the cache directory
Requires=user-cache-create.service

[Service]
ExecStart=/run/current-system/sw/bin/ln --symbolic --force /dev/shm/@{USER}@ @{HOME}@/.cache
User=%i

[Install]
WantedBy=default.target
