[Unit]
Description=Deluge Web Interface (Release Version)
Requires=deluged.service
After=network.target deluged-release.service user-cache.service

[Service]
Type=simple
User=%i
ExecStart=@(type -P deluge-web)@ \
  --loglevel info
Restart=on-failure

[Install]
WantedBy=default.target
