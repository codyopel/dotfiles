[Unit]
Description=Music Player Daemon
After=network.target sound.target user-cache-mount.service

[Service]
ExecStart=@(type -P mpd)@ --no-daemon @{XDG_CONFIG_HOME}@/mpd/mpd.conf
Restart=on-abort
User=%i
Group=users

[Install]
WantedBy=default.target
