[Unit]
Description=Music Player Daemon
After=network.target sound.target

[Service]
ExecStart=@{HOME}@/.nix-profile/bin/mpd --no-daemon @{HOME}@/.config/mpd/mpd.conf
Restart=on-abort
User=%i
Group=users

[Install]
WantedBy=default.target
