[Unit]
Description=Start tmux in detached session

[Service]
Type=forking
User=%I
ExecStart=${HOME}$/.nix-profile/bin/tmux new-session -s %u -d
ExecStop=${HOME}$/.nix-profile/bin/tmux kill-session -t %u

[Install]
WantedBy=default.target
