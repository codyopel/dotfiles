[Unit]
Description=Task Spooler Daemon
After=network.target user-cache.service

[Service]
Type=forking
User=%i
Group=users
ExecStart=@(type -P task-spooler)@
ExecStop=@(type -P task-spooler)@ -K
Restart=on-failure

[Install]
WantedBy=default.target
